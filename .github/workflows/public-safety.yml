name: public-safety

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Repository validation
        shell: pwsh
        run: pwsh -File scripts/validate-all.ps1